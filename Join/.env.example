# Django Configuration
SECRET_KEY=your-secret-key-here-generate-new-one
DEBUG=False
ALLOWED_HOSTS=your-api-domain.com,localhost,127.0.0.1

# Database (PostgreSQL for production, SQLite for development)
# DATABASE_URL=postgresql://username:password@hostname:port/database_name

# CORS/CSRF Origins (Frontend URLs only, NOT the API domain)
CORS_ALLOWED_ORIGINS=https://your-frontend-domain.com,http://localhost:5500,https://localhost:5500
CSRF_TRUSTED_ORIGINS=https://your-frontend-domain.com,http://localhost:5500,https://localhost:5500